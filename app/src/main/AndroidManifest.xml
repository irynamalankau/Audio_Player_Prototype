<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.audio_player_prototype">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:screenOrientation="portrait"
        android:label="@string/main_activity_label"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Musical_Player">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".AbbaActivity" android:label="@string/abba_activity_label"/>
        <activity android:name=".MetallicaActivity" android:label="@string/metallica_activity_label"/>
        <activity android:name=".QueenActivity" android:label="@string/queen_activity_label"/>
        <activity android:name=".PlayerActivity" android:label="@string/player_activity_label"/>
    </application>

</manifest>